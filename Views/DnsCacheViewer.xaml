<UserControl x:Class="Views.DnsCacheViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="#000000" Foreground="#00FF00" FontFamily="Consolas">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="DNS Cache Viewer" FontSize="24" FontWeight="Bold" Foreground="#00FF00" VerticalAlignment="Center" />
            <Button x:Name="btnScan" Content="Display DNS Cache" Margin="20,0,0,0" Padding="15,5" Cursor="Hand" Click="btnScan_Click" />
            <Button x:Name="btnFlush" Content="Flush DNS (Admin)" Margin="10,0,0,0" Padding="15,5" Background="#8B0000" Foreground="#FFFFFF" BorderThickness="0" Cursor="Hand" Click="btnFlush_Click" />
        </StackPanel>

        <TextBox x:Name="txtDnsOutput" Grid.Row="1" IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="#050505" Foreground="#00FF00" FontFamily="Consolas" BorderBrush="#00FF00" BorderThickness="1" Padding="10" />
    </Grid>
</UserControl>
