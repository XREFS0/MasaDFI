<UserControl x:Class="Views.PrefetchAnalyzer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="#000000" Foreground="#00FF00" FontFamily="Consolas">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Prefetch Analyzer" FontSize="24" FontWeight="Bold" Foreground="#00FF00" VerticalAlignment="Center" />
            <Button x:Name="btnScan" Content="Scan Prefetch Folder" Margin="20,0,0,0" Padding="15,5" Background="#000000" Foreground="#00FF00" FontWeight="Bold" BorderBrush="#00FF00" BorderThickness="1" Cursor="Hand" Click="btnScan_Click" />
        </StackPanel>

        <DataGrid x:Name="dgPrefetch" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Background="#050505" Foreground="#00FF00" RowBackground="#050505" AlternatingRowBackground="#0A0A0A" BorderBrush="#00FF00" BorderThickness="1" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Application Name" Binding="{Binding AppName}" Width="2*" />
                <DataGridTextColumn Header="Execution Hash" Binding="{Binding Hash}" Width="1*" />
                <DataGridTextColumn Header="Last Executed" Binding="{Binding LastExecuted}" Width="1.5*" />
                <DataGridTextColumn Header="File Size (Bytes)" Binding="{Binding Size}" Width="1*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
