<UserControl x:Class="Views.AdsScanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="#000000" Foreground="#00FF00" FontFamily="Consolas">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="ADS (Alternate Data Streams) Scanner" FontSize="24" FontWeight="Bold" Foreground="#00FF00" VerticalAlignment="Center" />
            <Button x:Name="btnScan" Content="Scan Directory" Margin="20,0,0,0" Padding="15,5" Background="#000000" Foreground="#00FF00" FontWeight="Bold" BorderBrush="#00FF00" BorderThickness="1" Cursor="Hand" Click="btnScan_Click" />
        </StackPanel>

        <DataGrid x:Name="dgAds" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Background="#050505" Foreground="#00FF00" RowBackground="#050505" AlternatingRowBackground="#0A0A0A" BorderBrush="#00FF00" BorderThickness="1" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File Path" Binding="{Binding FilePath}" Width="3*" />
                <DataGridTextColumn Header="Stream Name" Binding="{Binding StreamName}" Width="1.5*" />
                <DataGridTextColumn Header="Stream Size (Bytes)" Binding="{Binding StreamSize}" Width="1*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
