<UserControl x:Class="Views.ServicesAnalyzer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="#000000" Foreground="#00FF00" FontFamily="Consolas">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Windows Services Analyzer" FontSize="24" FontWeight="Bold" Foreground="#00FF00" VerticalAlignment="Center" />
            <Button x:Name="btnScan" Content="Scan Services" Margin="20,0,0,0" Padding="15,5" Cursor="Hand" Click="btnScan_Click" />
        </StackPanel>

        <DataGrid x:Name="dgServices" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Service Name" Binding="{Binding Name}" Width="1.5*" />
                <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" Width="2*" />
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="0.8*" />
                <DataGridTextColumn Header="Executable Path" Binding="{Binding PathName}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
